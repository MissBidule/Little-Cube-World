#version 330

// layout(triangles) in;
// layout(triangle_strip, max_vertices = 18) out;

// //Matrices de transformations
// uniform mat4 uVMatrixLight[6];

// out vec4 gFragPos;

void main() {
    // for (int face = 0; face < 6; face++)
    // {
    //     gl_Layer = face;
    //     for (int i = 0; i < 3; i++)
    //     {
    //         gFragPos = gl_in[i].gl_Position;
    //         gl_Position = uVMatrixLight[face] * gFragPos;
    //         EmitVertex();
    //     }
    //     EndPrimitive();
    // }
}
